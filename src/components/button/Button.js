import React, { Component } from 'react';
import './Button.css'

class Button extends Component {
  render() { 
    return (
      <button className="btn btn--outline">Entre em contato</button>
    );
  }
}

export default Button;
